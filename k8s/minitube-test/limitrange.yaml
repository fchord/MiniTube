apiVersion: v1
kind: LimitRange
metadata:
  name: minitube-test
  namespace: minitube-test
spec:
  limits:
    - type: Container
      default:
        cpu: "1"
        memory: 1Gi
      defaultRequest:
        cpu: 50m
        memory: 64Mi
