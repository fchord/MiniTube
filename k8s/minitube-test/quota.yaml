apiVersion: v1
kind: ResourceQuota
metadata:
  name: minitube-test
  namespace: minitube-test
spec:
  hard:
    pods: "16"
    persistentvolumeclaims: "4"
    requests.cpu: "4"
    requests.memory: 8Gi
    limits.cpu: "8"
    limits.memory: 16Gi
